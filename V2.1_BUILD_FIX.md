# V2.1 build fix

修复内容：

1. 修复 `BottomBar.kt` 旧导航调用漏传 `showLabels` 导致的 Release Kotlin 编译失败。
2. `hours_minutes` 改为位置参数，消除 Android 资源格式警告。
3. Android ICU `Transliterator` 的 `ThreadLocal.get()` 改为可空安全调用，兼容 Kotlin 2.3.x。
4. 删除清单中重复且互相矛盾的 `ACCESS_NETWORK_STATE tools:node="remove"` 声明。

构建命令：

```powershell
.\gradlew.bat assembleRelease
```

本轮不需要再次执行 `clean`。
