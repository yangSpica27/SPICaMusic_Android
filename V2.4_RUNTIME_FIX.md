# SPICaMusic v2.4：底栏收缩过渡图标拉伸与封面消失修复

本版本基于 `SPICaMusic_Android-userfix-v2.3` 修改，只处理首页上下滑时底栏在“展开双层布局”和“紧凑单层布局”之间切换的异常，不调整原有触发方式、动画时长、缓动、文字展开动画和全屏播放器形变。

## 本次修复

### 1. 当前页面图标不再被拉伸成整条导航栏

原实现把“圆形当前页面按钮”和“完整导航胶囊”作为同一个共享元素。共享元素从约 58dp 的圆形变为整条导航栏时，内部图标也会跟着父容器一起缩放，因此过渡中会短暂铺满一整栏。

本版将它拆成三个独立层：

- 导航栏纯背景：只负责圆形到长胶囊的原有形变；
- 当前页面选中底色：只负责移动到对应 Tab；
- 当前页面图标：保持固定 24dp，仅在两个位置之间移动。

因此仍保留 v2.3 的圆形→长胶囊动画，但任何导航图标都不会再被横向放大。

### 2. 正在播放封面不再突然消失

原实现把整条正在播放胶囊作为一个共享元素，而展开态和紧凑态内部结构不同，切换中封面会随整条 Row 被缩放、裁切或短暂丢失。

本版将播放栏拆成：

- 播放胶囊纯背景：继续执行原来的宽度与位置形变；
- 48dp 封面容器：作为独立共享元素，从原位置连续移动到新位置；
- 标题、歌手、播放按钮、队列按钮：保留原有进入/退出方式，不参与整栏缩放。

封面共享节点放在固定尺寸容器上，即使切歌交叉动画与底栏收缩同时发生，也不会生成两个同名共享节点。紧凑态原来的 220ms 图片交叉加载仍然保留。

### 3. 移除正在播放胶囊四周阴影

以下两种状态的播放胶囊 `shadowElevation` 均改为 `0.dp`：

- 展开双层布局中的长播放胶囊；
- 下滑后的紧凑单层播放胶囊。

描边、圆角、玻璃底色均保留，底部导航胶囊和“+”按钮的原有层级不做额外调整。

## 实现说明

共享过渡背景层关闭顶层 Overlay 绘制，避免纯背景在过渡中盖住文字和图标；当前页面图标与歌曲封面继续在 Overlay 中移动，并设置更高的过渡层级。这样背景只负责形变，真实图标只负责位移。

## 修改文件

```text
app/src/main/java/me/spica27/spicamusic/ui/home/player_bar/BottomBar.kt
app/src/main/java/me/spica27/spicamusic/ui/player/LargeBottomPlayerBar.kt
```

## 构建与安装

优先直接构建性能版：

```powershell
.\gradlew.bat --stop
.\gradlew.bat installPerformance --no-daemon
```

若 Android Studio 或 Gradle 仍使用旧缓存，再执行：

```powershell
.\gradlew.bat --stop
.\gradlew.bat clean installPerformance --no-daemon
```

APK 默认输出位置：

```text
app\build\outputs\apk\performance\app-performance.apk
```

## 真机验证重点

1. 首页连续上滑、下滑 10 次，观察“发现 / 音乐 / 资料库”的当前图标，应始终保持正常大小并移动到目标位置。
2. 观察正在播放封面，应从长胶囊中的位置连续移动到紧凑栏位置，不应变空、闪没或重新从另一处出现。
3. 在底栏切换过程中切换歌曲，确认封面加载动画与底栏过渡可以同时完成。
4. 检查展开态和紧凑态播放胶囊四周，均不再出现投影。
5. 检查上滑打开全屏播放器、下滑收回播放器，确认 v2.3 的形变动画和动态背景逻辑未被改变。

## 校验说明

已完成 Kotlin 文件的括号/字符串结构检查、修改差异检查、共享元素键配对检查、阴影参数检查和压缩包完整性检查。当前打包环境缺少可用的 Android SDK、Gradle 9.5 分发包及完整依赖缓存，因此无法在此处生成 APK；请在本机执行上述 `installPerformance` 做最终真机渲染验证。
