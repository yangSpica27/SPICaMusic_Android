# SPICaMusic v2.3：播放器闪屏、底栏文字与亮色细线修复

本版本基于 `SPICaMusic_Android-userfix-v2.2` 继续修改，针对 2026-07-27 的录屏和截图处理以下三个问题。

## 1. 正在播放打开、关闭时动态背景闪切

### 原因

上一版把全屏播放器分成“静态封面色背景”和“动态背景”两层，但动态背景使用 `GLSurfaceView` / `TextureView`。打开时动态 Surface 的首帧到达较晚，画面会先显示静态色，随后整层突然切到动态封面；关闭时则先停止或移除动态层，再显示静态色，因此两端都会像闪图一样跳一下。

### 修改

- `FluidWarp` 与 `EffectShader` 的 `GLSurfaceView` 在播放器收起后不再销毁。
- 收起后切换为 `RENDERMODE_WHEN_DIRTY`，冻结并保存最后一帧，不继续消耗 GPU。
- 展开前先恢复渲染并预绘制一帧，然后才开始长胶囊到全屏的形变。
- 动态 Surface 的真实 `View.alpha` 直接绑定形变进度，避免 `SurfaceView` 绕过 Compose 父层透明度。
- 首次真正产生动态首帧后，以 240ms 与静态兜底背景交叉淡化，不再一帧硬切。
- 关闭时动态背景跟随容器一起收回，等形变完全不可见后再冻结渲染。
- 播放器前景控件和动态背景分开控制：背景从形变前半段出现，控件在后半段淡入。

新的顺序：

```text
打开：
保留的动态最后一帧/静态兜底
→ 动态层随胶囊形变平滑显现
→ 全屏稳定
→ 播放器控件淡入

关闭：
播放器控件淡出
→ 动态层陪同全屏容器收回成长胶囊
→ 完全不可见后冻结动态 Surface
```

## 2. “发现 / 音乐 / 资料库”文字闪现

上一版为了避免文字在窄宽度时竖排，使用固定延迟后直接显示文字，因此展开完成时会突然闪现。

本版改为：

- 收起时立即从右向左缩回；
- 展开时文字容器从左向右逐步展开；
- 同时做轻微淡入；
- 文本始终保持单行、禁止换行；
- 不再使用 220ms 后直接切换可见状态的方案。

## 3. 亮色模式底栏内部出现细白线

细线不是单独绘制的分割线，而是底栏使用真实透明度时，页面正文或背景边界从胶囊后方透出来。暗色主题对比度低，所以不明显；亮色主题中会形成横向细线。

本版在亮色主题中采用“预合成玻璃色”：

- 先把 `surfaceContainer` 与页面背景按照原透明度比例混合；
- 最终以不透明颜色绘制；
- 视觉仍保持浅色玻璃混合感；
- 页面文字和边界无法再穿透，因此不会出现细白线。

暗色主题继续保留原来的真实半透明效果。

覆盖范围：

- 展开状态的迷你播放器；
- 收缩状态的播放器胶囊；
- 底部导航胶囊；
- 选中导航胶囊；
- 左右圆形操作按钮。

## 修改文件

```text
app/src/main/java/me/spica27/spicamusic/ui/home/player_bar/BottomBar.kt
app/src/main/java/me/spica27/spicamusic/ui/home/player_bar/BottomBarV2.kt
app/src/main/java/me/spica27/spicamusic/ui/player/ExpandedPlayerScreen.kt
app/src/main/java/me/spica27/spicamusic/ui/player/LargeBottomPlayerBar.kt
app/src/main/java/me/spica27/spicamusic/ui/widget/FluidMusicBackground.kt
app/src/main/java/me/spica27/spicamusic/ui/widget/FluidWarpBackground.kt
app/src/main/java/me/spica27/spicamusic/ui/widget/ShaderBackground.kt
```

## 构建与安装

本次只修改 Kotlin 源码，正常情况下不必每次执行 `clean`：

```powershell
.\gradlew.bat --stop
.\gradlew.bat installPerformance --no-daemon
```

`installPerformance` 会自动先构建再安装，不需要先单独执行 `assemblePerformance`。

若本地增量缓存导致显示仍像旧版本，再执行一次：

```powershell
.\gradlew.bat --stop
.\gradlew.bat clean installPerformance --no-daemon
```

Performance APK 输出位置：

```text
app\build\outputs\apk\performance\app-performance.apk
```

## 建议真机验证

1. 连续打开、关闭播放器 8～10 次，确认不再出现“纯色 ↔ 动态封面”的单帧跳变。
2. 在播放器完全展开前观察背景，动态画面应从胶囊阶段就平滑参与形变。
3. 上下滚动首页，让底栏在紧凑与展开状态间切换，文字应从左向右展开，而不是竖排或突然出现。
4. 使用亮色主题，把底栏覆盖在标题、段落或分割边界上，确认胶囊内部不再透出横向白线。
5. 切换到暗色主题，确认原有半透明层次仍保留。

## 本次校验说明

已完成修改文件的括号/字符串结构检查、调用参数检查、差异检查和压缩包完整性检查。当前打包环境没有可用的 Android SDK 与完整 Gradle 依赖缓存，因此最终 Android 编译和 Surface 渲染需要在本机执行上述 `installPerformance` 验证。
