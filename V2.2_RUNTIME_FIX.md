# SPICaMusic 用户修复版 v2.2

本轮针对 2026-07-27 01:33 录屏中的两个运行时问题：

## 1. 底部圆形按钮外侧出现多边形阴影

原因是圆形 `Surface` 在 `sharedElement` 快照中携带 6dp elevation 阴影，阴影超出圆形边界后被共享元素矩形范围裁切，在部分设备上会呈现近似多边形/方块的轮廓。

处理：

- 左侧紧凑导航按钮、展开态加号按钮、紧凑态加号按钮取消 elevation 阴影；
- 保留 1dp 描边、半透明材质和 1dp tonalElevation；
- 中间导航胶囊与迷你播放器胶囊仍保留原来的悬浮阴影，不影响整体突出感。

## 2. 每次打开正在播放页面都会先闪一下浅色页面

原因是上一轮动画在播放器展开后仍延迟 120ms 才创建动态背景；关闭时动态背景被停止。每次重新打开时，`FluidMusicBackground(active = false)` 都先返回纯 `surface`（亮色模式下接近白色），随后才切换到封面色动态背景，因此形成固定闪屏。

处理：

- 长胶囊形变到全屏时，容器颜色直接过渡到缓存的封面主色背景；
- 动态背景启动前始终绘制同色轻量渐变兜底，不再暴露白色 `surface`；
- 动态背景在播放器内容淡入前启动一帧，删除原来的 120ms 延迟；
- 关闭时先淡出播放器内容，再停止 GL/TextureView，最后用静态封面色容器收回成长胶囊；
- 动态背景设置默认状态由 `OFF` 改为项目实际默认的 `FluidWarp`，避免 ViewModel 首次发值前短暂显示关闭状态。

## 修改文件

- `app/src/main/java/me/spica27/spicamusic/ui/home/player_bar/BottomBar.kt`
- `app/src/main/java/me/spica27/spicamusic/ui/home/player_bar/BottomBarV2.kt`
- `app/src/main/java/me/spica27/spicamusic/ui/player/ExpandedPlayerScreen.kt`
- `app/src/main/java/me/spica27/spicamusic/ui/settings/SettingsViewModel.kt`
- `app/src/main/java/me/spica27/spicamusic/ui/widget/FluidMusicBackground.kt`

## 构建

第一次覆盖源码后执行：

```powershell
.\gradlew.bat --stop
.\gradlew.bat clean installPerformance --no-daemon
```

之后仅修改 Kotlin 文件时通常可直接执行：

```powershell
.\gradlew.bat installPerformance --no-daemon
```

APK 路径：

```text
app\build\outputs\apk\performance\app-performance.apk
```

## 真机检查

1. 上下滑动页面切换底栏紧凑/展开状态，确认左右圆形按钮外侧不再出现多边形轮廓。
2. 连续打开、关闭播放器 5 次，确认全屏形变过程一直保持封面色，不出现浅灰/白色整页闪帧。
3. 分别在亮色、暗色和跟随系统模式检查静态兜底色与动态背景衔接。
4. 在“动态背景”各模式（动态流体封面、流体效果、液态极光、顶部流光、模糊封面、关闭）各打开一次播放器。
